import mysql.connector

connector = mysql.connector.connect(user='vitali',
                                    password='1',
                                    host='localhost',
                                    database='chat')